# Database configuration for local PostgreSQL or Docker
# For local PostgreSQL, update user, password, host, port, and db name:
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/aml_db

# Environment settings
ENVIRONMENT=development
FUZZY_MATCH_THRESHOLD=65.0
